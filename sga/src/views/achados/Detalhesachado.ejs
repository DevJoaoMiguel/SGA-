<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalhes do Produto | SGA</title>
  <link rel="icon" href="/logo.svg" type="image/x-icon">

  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background-color: #f5f5f5;
      color: #333;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      padding: 0 20px;
    }

    .container {
      max-width: 1200px;
      width: 100%;
      display: flex;
      justify-content: space-between;
      background-color: #ffffff;
      border-radius: 16px;
      box-shadow: 0 6px 24px rgba(0, 0, 0, 0.1);
      padding: 40px;
      transition: box-shadow 0.3s ease-in-out;
      overflow: hidden;
      flex-wrap: wrap;
    }

    .container:hover {
      box-shadow: 0 12px 60px rgba(0, 0, 0, 0.15);
    }

    .product-img {
      max-width: 450px;
      width: 100%;
      border-radius: 12px;
      object-fit: cover;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease;
    }

    .product-img:hover {
      transform: scale(1.05);
    }

    .product-info {
      flex: 1;
      padding-left: 40px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .product-title {
      font-size: 38px;
      color: #1c1e3a;
      font-weight: 700;
      margin-bottom: 15px;
      letter-spacing: 1px;
      line-height: 1.3;
    }

    .product-price {
      font-size: 30px;
      font-weight: 600;
      color: #008C75;
      margin-bottom: 20px;
    }

    .product-type,
    .product-size {
      font-size: 18px;
      color: #444;
      margin-bottom: 12px;
      font-weight: 500;
    }

    .product-description {
      font-size: 16px;
      color: #555;
      margin-bottom: 40px;
      line-height: 1.8;
      max-width: 650px;
      text-align: justify;
    }

    .button-container {
      display: flex;
      gap: 20px;
      margin-top: 30px;
      flex-direction: column;
      align-items: flex-start;
    }

    .buy-button {
      padding: 18px 40px;
      background-color: #0077FF;
      color: white;
      font-size: 20px;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      width: 180%;
      max-width: 300px;
      text-align: center;
      transition: background-color 0.3s ease;
      box-shadow: 0 4px 16px rgba(0, 119, 255, 0.2);
    }

    .buy-button:hover {
      background-color: #005BB5;
    }

    .back-button {
      padding: 16px 35px;
      background-color: #5f6368;
      color: white;
      font-size: 18px;
      font-weight: 600;
      border-radius: 8px;
      text-align: center;
      text-decoration: none;
      transition: background-color 0.3s ease;
      width: 100%;
      max-width: 300px;
      text-align: center;
      box-shadow: 0 4px 16px rgba(95, 99, 104, 0.2);
    }

    .back-button:hover {
      background-color: #434848;
    }

    /* Responsive Design */
    @media (max-width: 1024px) {
      .container {
        flex-direction: column;
        align-items: center;
        padding: 30px 20px;
      }

      .product-img {
        max-width: 400px;
        margin-bottom: 20px;
      }

      .product-info {
        text-align: center;
        padding-left: 0;
      }

      .product-title {
        font-size: 32px;
      }

      .product-price {
        font-size: 26px;
      }

      .product-description {
        font-size: 15px;
      }

      .buy-button,
      .back-button {
        max-width: 100%;
      }
    }

    @media (max-width: 768px) {
      .container {
        padding: 20px;
      }

      .product-img {
        max-width: 300px;
      }

      .product-title {
        font-size: 24px;
      }

      .product-price {
        font-size: 22px;
      }

      .product-description {
        font-size: 14px;
      }

      .buy-button,
      .back-button {
        max-width: 100%;
      }
    }
  </style>
</head>





<body>
  <div id="page-spinner" class="spinner-overlay">
  <div class="spinner"></div>
</div>
<div class="container">
  <img src="/uploads/<%= achado.image %>" alt="<%= achado.title %>" class="achado-img">
  <div class="product-info">
    <h1 class="product-title"><%= achado.title %></h1>
    <p class="product-description"><%= achado.description %></p>
      <a href="/achado" class="back-button">Voltar para achados e perdidos</a>
    </div>
  </div>
</div>
<script>
  const spinner = document.getElementById("page-spinner");

  document.querySelectorAll("a[href]").forEach(link => {
    link.addEventListener("click", function (e) {
      const href = link.getAttribute("href");

      // Mostra o spinner apenas se o link não for âncora, JS ou com target
      if (
        !href.startsWith("#") &&
        !link.hasAttribute("target") &&
        !href.startsWith("javascript:")
      ) {
        spinner.style.display = "flex";
      }
    });
  });
</script>
</body>

</html>
